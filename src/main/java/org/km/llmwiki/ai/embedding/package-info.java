/**
 * Provider-neutral embedding contract for the Semantic / Vector foundation.
 *
 * <p>This package owns bounded canonical inputs, deterministic identities, finite vectors,
 * authoritative non-secret metadata, optional usage, and typed failure semantics. It deliberately
 * owns no persistence, sqlite-vec, KNN, retrieval, hybrid fusion, REST, or Browser behavior.
 * Provider-specific JSON and credentials stay inside an adapter package.
 */
package org.km.llmwiki.ai.embedding;
