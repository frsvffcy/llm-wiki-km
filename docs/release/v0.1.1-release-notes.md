# v0.1.1 Release notes (source)

> 本文件是 v0.1.1 patch release notes source（human-readable projection）。Executable API / schema authority
> 仍是 runtime contracts / Flyway / ADR / tests；本文件不得重新定義 executable authority。
> `v0.1.0` tag / release / artifacts 保持不可變；本 patch 不修改、不覆寫、不 retag `v0.1.0`。
> 真正建立 `v0.1.1` public tag / GitHub Release 仍需 human explicit authorization（A2）。
> Refs #454（parent：#448、#450、#451；acceptance owner：#429；artifact owner：#430）。

## Patch 內容（自 v0.1.0 行為差異）

- #448 workspace Document Analysis bootstrap / readiness / typed diagnostics：
  fresh workspace 建立時由 production `DocumentAnalysisPromptProvisioner` 自動 provision
  `config/prompts/document-analysis.md`；`GET /api/v1/analysis/readiness` 回報 `analysisReady=true`
 （`promptStatus=READY`、`provider=stub`、`model=offline` offline fallback）；missing / invalid prompt
  為 typed `PROMPT_TEMPLATE_NOT_FOUND` / `PROMPT_TEMPLATE_INVALID` 等，不再以整體看似 READY 掩蓋首次分析失敗。
  使用者無需手動建立 prompt、無需直接修改 SQLite。
- #450 Browser hidden visibility：
  全域 `[hidden]{display:none!important}` 恢復 `hidden` authority；`.empty-state{display:grid}` 不再覆寫隱藏語意。
  有資料時 empty state 不可同時顯示；空集合時才顯示空狀態。無 DOM / DB 偽造。
- #451 Inbox lifecycle vs extraction state UX / API contract：
  `document.status`（lifecycle：`PENDING` / `DUPLICATE` 等）與 `document.parse_status`（extraction：
  `PROCESSED` / `FAILED` / `UNSUPPORTED` / `NEED_OCR` / null=尚未抽取）分開顯示為雙徽章
  （`文件狀態` + `抽取狀態`）；`status=` 只篩 lifecycle，`parseStatus=` 只篩 extraction；extract 成功保持
  `status=PENDING` + `parseStatus=PROCESSED`（仍可 soft-delete），按鈕語意為「執行抽取」/「重新抽取」。
  抽取完成不再顯示「待處理」混淆。

## 升級指引（v0.1.0 → v0.1.1）

- 直接以 `v0.1.1` candidate JAR 啟動既有 knowledge root 即可；Flyway 無新 migration（沿用既有 chain），
  canonical `vault/` / `archive/` 不需手動遷移。
- 無需直接修改 SQLite 或手動建立 `config/prompts/document-analysis.md`；fresh workspace 會自動 bootstrap，
  既有 workspace 缺 prompt 可經 workspace repair 或還原該檔案（typed 診斷會指引路徑，不洩漏 absolute path）。
- Inbox 篩選：沿用 `status=` 者語意不變（仍為 lifecycle）；如需篩抽取狀態請改用 `parseStatus=`。
- Browser：無需清除特殊快取；empty-state 與雙徽章為純 projection 修正，無新 authority。

## Provider / config 邊界（維持既有正式文件）

- Answer provider（`app.ai.answer.*`）與 Document Analysis provider / config（`analysis` settings +
  `config/prompts/document-analysis.md` + `LlmClient`）為兩條獨立邊界；本 patch 不合併、不互換。
- 預設 Document Analysis 走 production-supported offline fallback（`provider=stub` / `model=offline`，
  `StubLlmClientConfiguration` `@ConditionalOnMissingBean`），非 test-only shortcut；live remote provider
  僅為額外互通性 evidence，不取代 deterministic acceptance。
- Credential 不得放在 JVM argv / `-D...api-key`；report / `ps` / log / screenshot / artifact 不得含 raw token。

## SUPPORTED

- LOCAL_ONLY（`server.address=127.0.0.1`，owner auth 可開可關；provider / backend disabled 不拖垮 baseline）。
- PRIVATE_INGRESS Mode 1（依 #422 / #423 hardened contract：canonical browser origin 四方對齊、
  versioned salted adaptive KDF verifier、bounded forwarder、raw backend loopback-only、
  wrong Host / Origin / transport fail closed）。
- Ingestion / extraction（含 bounded extraction ceilings、versioned chunking / normalization policies、
  Tika structured parse、CJK bigram lexical projection）。
- Document Analysis fresh-workspace journey（#454 §B：bootstrap → readiness READY → upload → extract →
  `POST /api/v1/analysis/jobs` → bounded poll → successCount>0 / failedCount=0，offline fallback）。
- Retrieval：FTS lexical、embedding projection lifecycle、vector KNN（需 sqlite-vec prerequisite）、
  hybrid fusion、second-stage exact-anchor rerank（default）、Evidence assembly、Retrieval Inspector、
  Source Chunk locator。
- Ask：stateless grounded answer（`grounded-answer@v2`）、citation validation、provider egress
  transparency（configuration + execution disclosure）、typed provider-disabled / retrieval failures。
- Governance：Ask → Proposal（explicit mutation command、citation currentness、per-workspace dedup）、
  Proposal → Draft → Human Review → explicit Publish、Published Wiki read（PUBLISHED-only、content hash）、
  Vault Lint read-only triage、governed repair ingress（`CANONICAL_CONTENT_INVALID` eligible only）。
- Browser first-mile smoke（#454 §C：static / Node contract + bounded manual checklist + packaged-artifact
  content gate；`[hidden]` authority + lifecycle / parseStatus 雙徽章在 exact candidate 中驗證）。
- MCP read-only loopback adapter（五個唯讀 tools，bounded transport-error envelope，無 write tools）。
- Operations：single-instance JAR + systemd primary、container mechanism（約束見 #418）、WAL-safe
  backup / restore operator procedures、deployment readiness projection、diagnostic redaction boundary。

## CANDIDATE / DISABLED BY DEFAULT

- Public HTTPS Mode 2（`REVERSE_PROXY_CANDIDATE`；contract 明確但永不回 SUPPORTED，需未來 adoption）。
- Query transformation single-rewrite（evaluation CONDITIONAL GO；production default 仍 disabled，
  需 versioned policy + exact-token protection + 另開 adoption issue）。
- Optional Graph projection（需 `GRAPH_PROJECTION_ENABLED` + ArcadeDB embedded prerequisite +
  readiness READY；unavailable 時 lexical + vector baseline + typed degradation）。
- Optional vector / semantic（需 `VECTOR_CAPABILITY_ENABLED` + pinned sqlite-vec native +
  embedding provider + projection READY；缺失為 typed unavailable，不破壞 baseline）。
- Container packaging（mechanism，有 single-instance / host-network / non-root 約束；primary 仍為 JAR + systemd）。
- Headless Browser automation（Playwright / Selenium；本 patch 不導入，僅在出現「無 headless 就無法可靠防回歸」
  的具體 evidence 時另開 framework-adoption Issue）。

## NOT SUPPORTED / DEFER

- Direct raw Internet bind（`0.0.0.0` / wildcard；validator fail-fast，REJECT）。
- OCR / scanned-PDF / layout-aware ingestion。
- MCP / agent write、autonomous publish / repair、auto-fix。
- Multi-user / RBAC / tenant、HA / multi-instance、PostgreSQL / Kubernetes。
- Maven Central / package registry publication、code-signing / notarization、native installer（DMG / MSI）、
  Docker registry image publication。
- Performance / SLA certification、live-provider quality / SLA guarantee。
- Query transformation default enablement、EXTRACTIVE compaction promotion、新 retrieval mode / ranking policy、
  claim-level contradiction ledger。
- 修改既有 `v0.1.0` release（retag / 覆寫 asset / 移動 tag 一律禁止）。

## Provenance

- Source commit / version (`0.1.1`) / Java / procedure (`release-candidate-procedure-v1`) /
  artifact SHA-256 / dependency fingerprint 見 `target/release-candidate/*-manifest.{json,md}`（machine + human readable）。
- `sourceCommit` 必須與 candidate run / tag dereference 一致；`v0.1.0` 保持不變。
- Reproducible-build timestamp 沿用 #430 authority（`2026-09-15T00:00:00Z`；version bump 不引入動態 build-time timestamp）。
- Dependency inventory 見 `*-dependencies.tsv`（deterministic coordinates；較弱選項，非 CycloneDX）。
- Native prerequisites 見 `docs/release/native-capability-matrix.md`（pinned v0.1.9 + checksums）。
- Product-journey evidence 見 `target/release-evidence/`（#429 + #454 §B Document Analysis path；failure / skip 阻止 READY_TO_PUBLISH）。
- Browser first-mile evidence 見 Node contracts（`hidden-visibility` + `inbox-ui`）+ packaged-artifact content gate +
  versioned manual checklist（`docs/release/v0.1.1-browser-smoke-checklist.md`）。
- 本 patch 完成（FULL GO）停在 `READY_TO_PUBLISH`，不等於 publish 授權；publish 需 A2 explicit human authorization。
