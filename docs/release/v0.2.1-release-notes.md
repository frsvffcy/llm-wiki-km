# v0.2.1 Release notes (source)

> 本文件是 v0.2.1 release notes source（human-readable projection）。Executable API / schema authority
> 仍是 runtime contracts / Flyway / ADR / tests；本文件不得重新定義 executable authority。
> `v0.1.0` / `v0.1.1` / `v0.2.0` tags / releases / artifacts 保持不可變；本 release 不修改、不覆寫、不 retag 歷史版本。
> 真正建立 `v0.2.1` public tag / GitHub Release 仍需 human explicit authorization（A2）。
> Refs #553（parent Sprint）、#554（identity rebaseline）、#555（exact-candidate acceptance）。

## 自 v0.2.0 行為差異

- #554 release identity rebaseline：Maven project version `0.2.0` → `0.2.1`；
  runtime / artifact / manifest / readiness 單一 authority chain 不變（`version.properties` +
  `ApplicationVersion` + `Implementation-Version` 四方一致），不新增第二套版本真相。
- #517 post-release corrective（v0.2.0 正式發布後唯一的 production runtime 變更）：
  `src/main/resources/static/inbox-ui.js` mutation handler 在 shared `inFlight` guard 仍為 true 時呼叫
  `refresh()` 導致 follow-up refresh 被 early return；修正後 single upload、extraction、batch upload、
  rescan、remove 成功後自動重新 GET 並 render 最新 backend state，不再需要手動「套用篩選」；
  filter / page state 保留，in-flight guard 仍阻止重複操作，typed failure 不顯示假成功。
- post-release test / evaluation hardening（不等於新 production feature）：#541 retrieval evaluation trust contract
 （substrate liveness、environment stamp、channel attribution）、#546 query-shape coverage matrix
 （CONFLICTING_INFO / COMPLETENESS_REQUIRED deterministic truth）、#551 grounded-answer answer-level
  citation-coverage evaluation，以及 Wenlan / Onyx / VikingRAG / Graphify / MiroFish / COG / Dream-RSI /
  historical-trace-replay 等 evaluation lineage 收斂；均為 evaluation / test / docs hardening，
  未改 production retrieval / ranking / prompt / Answer default。
- #541 / #546 / #551 明確未改 production retrieval / Answer default：不改 `EvidenceBundle.insufficientEvidence`
  empty-items 語意，不改 `grounded-answer@v2` default，不改 `GroundedAnswerResponseContract`，
  不新增 adaptive / multi-round retrieval，不新增 requiredEvidenceIds public API。
- #533 adaptive retrieval trigger 仍未成立；本 patch 不偷渡 adaptive retrieval、multi-round retrieval、
  grounded-answer prompt default change、query transformation default switch、OCR / layout-aware provider、
  MCP write / auto-memory / auto-publish、second writer CLI、Graph authority rewrite、public Internet raw bind，
  或任何尚未有 trigger 的 external-evaluation candidate。
- Flyway：自 v0.2.0 無新增 migration（最高仍為 `V34__add_normalization_policy_version.sql`）；
  既有 knowledge root 以 `v0.2.1` candidate JAR 直接啟動即可，無手動遷移。

## 升級指引（v0.2.0 → v0.2.1）

- 直接以 `v0.2.1` candidate JAR 啟動既有 knowledge root 即可；Flyway 無新 migration（沿用既有 chain），
  canonical `vault/` / `archive/` 不需手動遷移。
- 無需直接修改 SQLite 或手動處理 prompt / version 檔案；版本真相只在 `pom.xml`，
  runtime 版本由 packaged JAR 內部雙 identity 與 booted runtime 四方一致證明。
- Browser：升版後一般重新整理即取得新版 static assets（#481 content-hash query token 沿用）；
  Inbox mutation 成功後清單自動刷新（#517），不再需要手動再按篩選；Inbox 雙徽章（lifecycle /
  parseStatus）與 `[hidden]` 語意維持 #450 / #451 contract。
- Inbox 篩選語意不變：`status=` 仍為 lifecycle，抽取狀態請用 `parseStatus=`。

## Provider / config 邊界（維持既有正式文件）

- Answer provider（`app.ai.answer.*`）與 Document Analysis provider / config（`analysis` settings +
  `config/prompts/document-analysis.md` + `LlmClient`）為兩條獨立邊界；本 release 不合併、不互換。
- 預設 Document Analysis 走 production-supported offline fallback（`provider=stub` / `model=offline`，
  `StubLlmClientConfiguration` `@ConditionalOnMissingBean`），非 test-only shortcut；live remote provider
  僅為額外互通性 evidence，不取代 deterministic acceptance。
- Credential 不得放在 JVM argv / `-D...api-key`；report / `ps` / log / screenshot / artifact 不得含 raw token。

## SUPPORTED

- LOCAL_ONLY（`server.address=127.0.0.1`，owner auth 可開可關；provider / backend disabled 不拖垮 baseline）。
- PRIVATE_INGRESS Mode 1（依 #422 / #423 hardened contract：canonical browser origin 四方對齊、
  versioned salted adaptive KDF verifier、bounded forwarder、raw backend loopback-only、
  wrong Host / Origin / transport fail closed）。
- Ingestion / extraction（含 bounded extraction ceilings、versioned chunking / normalization policies、
  Tika structured parse、CJK bigram lexical projection）。
- Document Analysis fresh-workspace journey（bootstrap → readiness READY → upload → extract →
  `POST /api/v1/analysis/jobs` → bounded poll → successCount>0 / failedCount=0，offline fallback）。
- Retrieval：FTS lexical、embedding projection lifecycle、vector KNN（需 sqlite-vec prerequisite）、
  hybrid fusion、second-stage exact-anchor rerank（default）、Evidence assembly、Retrieval Inspector、
  Source Chunk locator。
- Ask：stateless grounded answer（`grounded-answer@v2`）、citation validation（含 superseded /
  deleted / duplicate source fail-closed）、provider egress
  transparency（configuration + execution disclosure）、typed provider-disabled / retrieval failures。
- Governance：Ask → Proposal（explicit mutation command、citation currentness、per-workspace dedup）、
  Proposal → Draft → Human Review → explicit Publish、Published Wiki read（PUBLISHED-only、content hash）、
  Vault Lint read-only triage、governed repair ingress（`CANONICAL_CONTENT_INVALID` eligible only）。
- Browser first-mile（含 #493 UX 與 #517 Inbox mutation auto-refresh：design tokens、導覽 IA、
  跨頁面 Empty / Loading / Success / typed Error / Disabled、content-hash static versioning、
  `[hidden]` authority + lifecycle / parseStatus 雙徽章在 exact candidate 中驗證）。
- 公開 API 錯誤訊息語言治理（#504：`error.message` 繁體中文固定投影，typed code / status 不被壓平）。
- MCP read-only loopback adapter（五個唯讀 tools，bounded transport-error envelope，無 write tools）。
- Operations：single-instance JAR + systemd primary、container mechanism（約束見 #418）、WAL-safe
  backup / restore operator procedures、deployment readiness projection、diagnostic redaction boundary。

## CANDIDATE / DISABLED BY DEFAULT

- Public HTTPS Mode 2（`REVERSE_PROXY_CANDIDATE`；contract 明確但永不回 SUPPORTED，需未來 adoption）。
- Query transformation single-rewrite（evaluation CONDITIONAL GO；production default 仍 disabled，
  需 versioned policy + exact-token protection + 另開 adoption issue）。
- Optional Graph projection（需 `GRAPH_PROJECTION_ENABLED` + ArcadeDB embedded prerequisite +
  readiness READY；unavailable 時 lexical + vector baseline + typed degradation）。
- Optional vector / semantic（需 `VECTOR_CAPABILITY_ENABLED` + pinned sqlite-vec native +
  embedding provider + projection READY；缺失為 typed unavailable，不破壞 baseline）。
- Container packaging（mechanism，有 single-instance / host-network / non-root 約束；primary 仍為 JAR + systemd）。
- Headless Browser automation（Playwright / Selenium；本 release 不導入，僅在出現「無 headless 就無法可靠防回歸」
  的具體 evidence 時另開 framework-adoption Issue）。

## NOT SUPPORTED / DEFER

- Direct raw Internet bind（`0.0.0.0` / wildcard；validator fail-fast，REJECT）。
- OCR / scanned-PDF / layout-aware ingestion。
- MCP / agent write、autonomous publish / repair、auto-fix。
- Multi-user / RBAC / tenant、HA / multi-instance、PostgreSQL / Kubernetes。
- Maven Central / package registry publication、code-signing / notarization、native installer（DMG / MSI）、
  Docker registry image publication。
- Performance / SLA certification、live-provider quality / SLA guarantee。
- Query transformation default enablement、EXTRACTIVE compaction promotion、新 retrieval mode / ranking policy、
  claim-level contradiction ledger。
- Code-intelligence sidecar default（#505 `DEFER`）、external observability mandatory adoption、
  first-party second-writer CLI、claim-level ledger。
- 修改既有 `v0.1.0` / `v0.1.1` / `v0.2.0` releases（retag / 覆寫 asset / 移動 tag 一律禁止）。

## Provenance

- Source commit / version (`0.2.1`) / Java / procedure (`release-candidate-procedure-v1`) /
  artifact SHA-256 / dependency fingerprint 見 `target/release-candidate/*-manifest.{json,md}`（machine + human readable）。
- `sourceCommit` 必須與 candidate run / manifest 一致；`v0.1.0` / `v0.1.1` / `v0.2.0` 保持不變。
- Reproducible-build timestamp 沿用 #430 authority（`2026-09-15T00:00:00Z`；#554 rebaseline 不引入動態 build-time timestamp，它只是 reproducibility input，不代表 release date / currentness）。
- Dependency inventory 見 `*-dependencies.tsv`（deterministic coordinates；較弱選項，非 CycloneDX）。
- Native prerequisites 見 `docs/release/native-capability-matrix.md`（pinned v0.1.9 + checksums）。
- Product-journey evidence 見 `target/release-evidence/`（#429 + #454 §B Document Analysis path；failure / skip 阻止 READY_TO_PUBLISH）。
- Browser first-mile evidence 見 Node contracts（`hidden-visibility` + `inbox-ui`）+ packaged-artifact content gate +
  versioned historical checklist（`docs/release/v0.1.1-browser-smoke-checklist.md` 為 v0.1.1 source）。
- 本 release 完成（FULL GO）停在 `READY_TO_PUBLISH`，不等於 publish 授權；publish 需 A2 explicit human authorization。
